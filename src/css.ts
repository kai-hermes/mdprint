// mdprint — the print stylesheet, verbatim from the POC.
//
// Copied, not rewritten. Every rule in here was paid for with a bad sheet of
// paper (see SPEC §2). Treat it as data, not as code you can tidy: the tests in
// test/css.test.ts assert the load-bearing rules are still present, because the
// single most likely refactor accident is someone deleting a fix that looks
// redundant.

export const PRINT_CSS: string = `
/* mdprint.css — print stylesheet\n   Derived from explain-diff-html's earned rules + the missing @page block.\n   Standalone: works as a linked file OR inlined into the HTML. No deps. */\n\n/* ============================================================\n   1. TOKENS — pinned so dark-mode readers still print light\n   ============================================================ */\n:root{\n  --ink:#14161a;\n  --ink-soft:#4a5058;\n  --ink-faint:#8a929c;\n  --rule:#d8dde3;\n  --rule-soft:#e8ecf0;\n  --panel:#f5f7f9;\n  --accent:#1f6feb;\n  --accent-soft:#e8f1ff;\n  --warn:#8a5a00;\n  --warn-soft:#fff6e0;\n  --font-body:-apple-system,BlinkMacSystemFont,"Segoe UI",Roboto,Helvetica,Arial,sans-serif;\n  --font-mono:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;\n  --measure:17px;\n  --leading:1.65;\n}\n\n/* ============================================================\n   2. @page — the block explain-diff-html never had\n   Page size + margins are set here, NOT left to the print dialog.\n   ============================================================ */\n@page{\n  size:A4;\n  margin:14mm 15mm 16mm 15mm;\n}\n/* Roomier inside margin for stapling/binding */\n@page :left  { margin-left:20mm; }\n@page :right { margin-right:20mm; }\n\n/* First page gets no running header */\n@page :first{ margin-top:12mm; }\n\nhtml{\n  -webkit-text-size-adjust:100%;\n}\n\nbody{\n  margin:0;\n  padding:0;\n  background:#fff;\n  color:var(--ink);\n  font-family:var(--font-body);\n  font-size:var(--measure);\n  line-height:var(--leading);\n  /* Screen only: keep text off the window edges. Print uses @page margins. */\n  max-width:44rem;\n  margin-inline:auto;\n  padding:32px 24px 64px;\n}\n\n/* ============================================================\n   3. PRINT — the rules that cost real bugs to learn\n   ============================================================ */\n@media print{\n  /* (a) Grid and horizontal flex do not fragment across printed pages —\n         content past page 1 silently vanishes. Force block layout. */\n  body{display:block;max-width:none;padding:0;font-size:10.5pt;line-height:1.45;}\n  main,article,section,div,header,footer{\n    display:block !important;\n    grid-template-columns:none !important;\n    float:none !important;\n  }\n\n  /* (b) Pin EVERY token to its light value. A reader in dark mode must\n         still get a light, ink-frugal page. */\n  :root{\n    --ink:#000;\n    --ink-soft:#333;\n    --ink-faint:#666;\n    --rule:#b9c0c8;\n    --rule-soft:#dde2e7;\n    --panel:#f7f8f9;\n    --accent:#14508f;\n    --accent-soft:#eef4fc;\n    --warn:#6b4600;\n    --warn-soft:#fdf7e8;\n  }\n\n  /* (c) Colours and backgrounds must survive the print dialog */\n  html,body{background:#fff !important;}\n  *{-webkit-print-color-adjust:exact;print-color-adjust:exact;}\n\n  /* (d) Show things that are collapsed on screen */\n  [hidden]{display:revert !important;}\n  details{display:block !important;}\n  details > *{display:block !important;}\n\n  /* (e) Never strand a heading at the foot of a page */\n  h1,h2,h3,h4,h5,h6{\n    break-after:avoid;page-break-after:avoid;\n    break-inside:avoid;page-break-inside:avoid;\n  }\n  h2{\n    break-before:auto;\n    margin-top:1.1em;\n    border-bottom:1px solid var(--rule);\n    padding-bottom:.15em;\n  }\n  /* Do not open a page with a lone heading */\n  h2:not(:first-of-type){break-before:auto;}\n\n  /* (f) Keep blocks whole — the bug that split code mid-page */\n  pre,blockquote,table,figure{\n    break-inside:avoid;page-break-inside:avoid;\n  }\n  pre{\n    /* Long lines wrap instead of bleeding off the paper edge */\n    white-space:pre-wrap;\n    overflow-wrap:anywhere;\n    word-break:break-word;\n  }\n\n  /* (g) Widows/orphans — no single dangling lines */\n  p,li{orphans:3;widows:3;}\n\n  /* (h) Links: show the target, since paper can't be clicked */\n  a{color:var(--ink);text-decoration:none;border-bottom:1px solid var(--rule-soft);}\n  a[href^="http"]::after{\n    content:" (" attr(href) ")";\n    font-size:.78em;\n    color:var(--ink-faint);\n    word-break:break-all;\n  }\n  /* ...except when the link text already IS the URL */\n  a[href^="http"]:has(> code)::after,\n  a[href^="http"].bare::after{content:"";}\n\n  /* (i) Drop screen-only chrome */\n  .no-print,nav,.toc-toggle{display:none !important;}\n\n  /* (j) The running footer — version tracking on every page */\n  .page-footer{display:block !important;}\n}\n\n/* ============================================================\n   4. COMPONENTS\n   ============================================================ */\nh1,h2,h3,h4{line-height:1.25;font-weight:650;}\nh1{\n  font-size:1.9em;\n  margin:0 0 .35em;\n  letter-spacing:-.01em;\n}\nh2{\n  font-size:1.3em;\n  margin:1.8em 0 .6em;\n  padding-bottom:.2em;\n  border-bottom:2px solid var(--accent);\n}\nh3{font-size:1.08em;margin:1.4em 0 .45em;}\nh4{font-size:.95em;margin:1.2em 0 .35em;color:var(--ink-soft);}\np{margin:0 0 .85em;}\n\n/* Preamble: what you see above the first rule */\n.preamble{\n  color:var(--ink-soft);\n  font-size:.94em;\n  border-left:3px solid var(--accent);\n  padding:.1em 0 .1em .9em;\n  margin:0 0 1.6em;\n}\n\n/* Tables — must stay put and read straight down the page */\ntable{\n  width:100%;\n  border-collapse:collapse;\n  margin:1em 0 1.3em;\n  font-size:.92em;\n}\nthead{\n  /* Repeats on every page if the table splits */\n  display:table-header-group;\n}\ntfoot{display:table-footer-group;}\nth{\n  text-align:left;\n  font-weight:650;\n  background:var(--panel);\n  border-bottom:1.5px solid var(--rule);\n  padding:6px 9px;\n}\ntd{\n  border-bottom:1px solid var(--rule-soft);\n  padding:6px 9px;\n  vertical-align:top;\n}\ntr{break-inside:avoid;page-break-inside:avoid;}\ntd code{font-size:.9em;}\n\n/* Callouts */\n.callout{\n  background:var(--accent-soft);\n  border-left:3px solid var(--accent);\n  padding:.7em .9em;\n  margin:1em 0 1.2em;\n  break-inside:avoid;\n}\n.callout.warn{background:var(--warn-soft);border-left-color:var(--warn);color:var(--warn);}\n.callout *:last-child{margin-bottom:0;}\n\n/* Blockquote */\nblockquote{\n  margin:1em 0 1.2em;\n  padding:.1em 0 .1em 1em;\n  border-left:2px solid var(--rule);\n  color:var(--ink-soft);\n  font-style:italic;\n}\n\n/* Code */\ncode{\n  font-family:var(--font-mono);\n  font-size:.88em;\n  background:var(--panel);\n  padding:.1em .3em;\n  border-radius:3px;\n}\npre{\n  font-family:var(--font-mono);\n  font-size:.84em;\n  line-height:1.5;\n  background:var(--panel);\n  border:1px solid var(--rule-soft);\n  border-radius:4px;\n  padding:.75em .9em;\n  margin:1em 0 1.2em;\n}\n/* Screen: long lines scroll inside the box */\npre{overflow-x:auto;overflow-y:hidden;}\n@media print{\n  /* Paper: no scrollbar exists, so the box has to wrap instead of clip.\n     overflow:hidden also stops the scrollable box from being sliced at\n     the page break. */\n  pre{overflow:visible !important;}\n  pre code{white-space:pre-wrap !important;overflow-wrap:anywhere !important;word-break:break-word !important;}\n}\npre code{background:none;padding:0;font-size:1em;}\n\n/* Filename label — must never strand above a code block on the next page */\n.filename{\n  font-family:var(--font-mono);\n  font-size:.8em;\n  color:var(--ink-faint);\n  margin:1em 0 -.4em;\n  break-after:avoid;page-break-after:avoid;\n}\n\n/* Lists */\nul,ol{margin:0 0 .9em;padding-left:1.5em;}\nli{margin:.25em 0;}\nli > ul,li > ol{margin:.25em 0;}\n/* Task lists (checkbox syntax) */\nli.task{list-style:none;margin-left:-1.2em;}\nli.task::before{\n  content:"☐ ";\n  font-family:var(--font-mono);\n  color:var(--ink-faint);\n}\nli.task.done::before{content:"☑ ";color:var(--accent);}\nli.task.done{color:var(--ink-faint);text-decoration:line-through;}\n\nhr{border:0;border-top:1px solid var(--rule);margin:2em 0;}\n\nimg{max-width:100%;height:auto;break-inside:avoid;}\n\n/* Section rhythm marker for print */\n.section-rule{display:none;}\n\n/* ============================================================\n   5. Page footer — title + date on every page\n   Browsers can't repeat arbitrary HTML per page, so the footer\n   is a fixed-position element that the print engine repeats.\n   ============================================================ */\n.page-footer{\n  display:none; /* screen: hidden */\n  position:fixed;\n  bottom:0;\n  left:0;\n  right:0;\n  font-size:8pt;\n  color:var(--ink-faint);\n  border-top:1px solid var(--rule-soft);\n  padding-top:3px;\n  /* Leave room above the paper edge */\n  margin-bottom:-8mm;\n}\n.page-footer .pf-right{float:right;}
`;
